@model IEnumerable<JamshidDashBoard.Models.Monitor>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PersonName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PersonalPageUrl)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PersonName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PersonalPageUrl)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MonitorID }) |
            @Html.ActionLink("Details", "Details", new { id=item.MonitorID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MonitorID })
        </td>
    </tr>
}

</table>
